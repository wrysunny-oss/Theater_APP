# 幻乐剧场项目功能说明

## 用户类型与后台访问

- 普通用户：通过 App 注册和登录，不允许登录中后台。
- 代理用户：只能由管理员创建，可登录中后台；仅能查看自己邀请码邀请的全部层级下级成员及这些成员的广告收益，不能新增、编辑、删除、审核或调整资金。
- 管理员用户：拥有后台角色并按角色权限访问中后台。超级管理员可维护全部业务数据。
- 代理后台的只读限制同时由按钮权限、接口权限和服务端数据范围校验保证，不能通过直接调用接口绕过。
- 代理概览提供今日/本月/累计广告收入、今日新增成员、30 日活跃成员、今日/本月/累计代理佣金、近 7 日收益趋势和下级成员贡献排行。
- 代理成员列表标记成员相对代理的真实邀请层级，范围覆盖邀请码名下的全部深度下级。
- 用户需在设置中预先绑定收款账户，完整姓名和账号由服务端加密保存；提现页不再填写或提交账户信息，订单自动使用绑定账户并保存申请时快照。
- GroMore 激励视频使用广告位 `104489019`，服务端按官方 GET 回调、`sha256(m-key:trans_id)` 验签和 `trans_id` 幂等；eCPM 按分/千次展示换算为单次人民币预估收入后进入广告分成。

> 文档版本：2026-09-04  
> 本文以当前仓库代码和数据库结构为准，用于产品确认、研发维护、测试验收和后续第三方对接。

## 1. 项目概述

幻乐剧场由三个独立项目组成：

| 项目 | 技术栈 | 默认地址 | 职责 |
| --- | --- | --- | --- |
| Frontend | uni-app、Vue 3、TypeScript、uView Plus | `http://127.0.0.1:5173` | APP/H5 用户端 |
| Admin | Vue Vben Admin、Naive UI、TypeScript | `http://127.0.0.1:5888` | 运营及财务管理后台 |
| Backend | Express、TypeScript、Prisma、MySQL | `http://127.0.0.1:3000` | 统一业务 API、鉴权及数据持久化 |

后端接口统一前缀为 `/api/v1`，Swagger 文档地址为 `http://127.0.0.1:3000/docs`，健康检查为 `/health`，数据库就绪检查为 `/ready`。

## 2. APP 用户端

### 2.1 登录与注册

- APP 不允许游客访问业务页面。
- 未登录冷启动、重新进入前台或跳转业务页面时，统一进入登录注册页。
- 未登录状态仅允许访问登录注册、用户协议和隐私政策页面。
- 使用手机号和密码登录。
- 新用户注册必须填写有效邀请码。
- 邀请码为 6 至 12 位字母或数字，服务端统一转换为大写。
- 注册时验证邀请人并在同一事务中建立邀请关系；无效邀请码不会创建账号。
- 访问令牌过期后使用一次性刷新令牌轮换；刷新失败时清理会话并重新登录。
- 支持退出登录和旧密码验证后的密码修改。

### 2.2 用户资料

- 查看和修改昵称、性别、生日及个人简介。
- 从相册选择或拍照上传头像。
- 支持 JPEG、PNG、WebP、GIF，单文件最大 5MB。
- 头像通过鉴权接口上传，地址写入用户表，重新登录后仍可显示。

### 2.3 短剧与播放器

- 首页运营轮播、推荐位、公告和启动配置由后端下发。
- 支持短剧列表、分类或关键词查询及短剧详情。
- 支持自定义播放页、选集和播放进度展示。
- 支持收藏、取消收藏、收藏列表、观看历史及断点进度保存接口。
- 短剧内容计划接入穿山甲内容输出，正式内容源及生产播放参数仍待平台流程完成。

### 2.4 福利与金币

- 奖励中心展示签到状态、连续签到天数和签到规则。
- 每日签到由后端按北京时间校验并幂等发放奖励。
- 金币明细区分签到、任务、广告、分享、提现及人工调整等类型。
- 收入显示正号，支出显示负号并使用红色标识。
- 金币余额和不可变流水在同一事务中更新。

### 2.5 邀请关系

- 每个用户都有唯一邀请码，也可以修改自己的邀请码。
- 修改后的邀请码必须全局唯一。
- 已建立的邀请关系保留绑定时的邀请码快照，不受后续改码影响。
- 每个用户只能绑定一次上级，不能绑定自己。
- 每个人既是普通用户，也可能成为其他用户的直属或隔级推荐人；层级是相对关系，不是固定身份。
- 管理员可以在后台直接创建无需上级邀请码的一级代理账号，系统自动生成该账号自己的邀请码。

### 2.6 提现

- 展示提现规则、金币余额和个人提现记录。
- 支持支付宝渠道资料及提现申请。
- 服务端校验最低/最高金币、每日次数、每日限额及账户风险状态。
- 提交后冻结金币；审核拒绝或打款失败时释放冻结金币。

### 2.7 帮助与反馈

- 查看已发布的用户协议、隐私政策和帮助文档。
- 提交问题反馈、联系方式和图片证据。
- 查看自己的反馈状态及后台回复。

## 3. 广告收入与三级返佣

### 3.1 配置项

- 全局用户分成比例：所有未设置独立比例的用户继承该值。
- 用户独立分成比例：在后台用户详情配置；留空表示继承全局。
- 全局直推返佣比例。
- 全局间推返佣比例。
- 每项比例使用万分比整数存储，前端以百分比展示，支持精确到 `0.01%`。
- 用户分成比例范围为 0% 至 100%。
- 直推与间推返佣比例之和不能超过 100%。

### 3.2 单次广告结算公式

广告人民币收入精确保存到百万分之一元，金币换算复用提现配置中的“每人民币分所需金币”。整个计算过程使用整数，不使用浮点金额。

```text
基础应分金币 = 广告收入（元）× 每元金币数 × 用户分成比例

直属上级返佣 = 基础应分金币 × 直推返佣比例
隔级上级返佣 = 基础应分金币 × 间推返佣比例

观看用户实际到账 = 基础应分金币
平台额外支付佣金 = 直属上级返佣 + 隔级上级返佣
```

- 直推和间推返佣均不从观看用户收益中扣除，由平台作为额外成本支付。
- 观看用户没有对应上级时，平台不产生该级返佣支出。
- 观看者、直属上级和隔级上级的金币在同一个数据库事务中入账。
- 每一方分别生成“观看广告收益”“直推用户广告分成”或“间推用户广告分成”流水。
- 单次结算使用唯一 `requestId`，穿山甲重复回调不会重复发币。
- 结算记录保存广告原始收入、兑换率、三级比例、用户完整收益、两级返佣及资金承担方快照。

### 3.3 循环层级说明

假设 A 邀请 B，B 邀请 C。C 观看广告时：C 是观看用户，B 是直属上级，A 是隔级上级。

当 B 自己观看广告时，B 又成为观看用户，A 成为 B 的直属上级。B 或 C 继续邀请其他用户后，也会在各自的下级网络中获得直推、间推返佣，因此相同规则可以在整棵邀请关系中循环使用，但每一笔广告最多向上结算两级。

每个用户的累计收益可以表达为：

```text
用户累计收益 =
本人历次观看广告完整收益
+ 所有直属下级贡献的直推返佣
+ 所有隔级下级贡献的间推返佣
+ 其他任务及运营奖励
```

### 3.4 代理无限下级分成

- 代理账号只能由具备用户管理权限的管理员创建，普通用户不能申请或自行升级为代理。
- 创建代理时必须配置独立代理分成比例，后续可在代理用户详情中单独调整，比例使用万分比保存。
- 代理本人观看广告只按普通观看用户规则获得收益，不对自己的广告收益计算代理佣金。
- 代理邀请码邀请的直属用户，以及这些用户继续邀请形成的任意深度下级，观看广告时都会触发该代理佣金。
- 代理佣金以观看用户的基础广告收益为基数：`代理佣金 = 下级基础广告收益 × 代理独立分成比例`。
- 如果一条邀请祖先链存在多名代理，每名代理都按各自比例获得平台额外佣金，互不扣减。
- 代理佣金由平台额外支付，不减少观看用户、直推用户或间推用户已经获得的收益。
- 每笔代理佣金保存广告结算、代理、来源用户、邀请深度、比例和金额快照，并生成独立金币流水。
- 禁用、封禁、冻结或奖励受限的代理暂停获得新代理佣金；恢复正常后仅参与后续新结算，不追补历史。

## 4. 管理后台

### 4.1 运营概览

- 注册用户、累计金币、已发布短剧和待处理反馈统计。
- 展示资金对账异常数量和最近对账结果。

### 4.2 用户管理

- 分页查询用户，支持手机号和昵称搜索。
- 查看基础资料、角色、金币余额、收藏数、观看历史数及最近流水。
- 启用或禁用用户。
- 人工增减金币，余额、流水和审计日志同事务提交。
- 设置用户独立广告分成比例，或恢复继承全局比例。
- 创建无需上级邀请码的一级代理用户。

### 4.3 奖励运营

- 配置注册、直接邀请、间接邀请和连续签到奖励。
- 配置全局观看用户分成、直推返佣和间推返佣。
- 查看邀请关系列表和签到记录。
- 提供可信广告收入手动结算接口，供穿山甲正式回调接入前验证结算公式。

### 4.4 资金管理

- 查看不可变金币流水。
- 配置金币兑换比例、提现开关、限额、次数和手续费。
- 查询、审核、拒绝及确认提现订单。
- 创建打款批次、导出敏感收款 CSV、导入结果预检和确认入账。
- CSV 防公式注入，导出禁用缓存，批次及结果请求支持幂等。
- 定时或手动执行余额对账并生成异常记录。

### 4.5 运营配置

- 配置首页轮播、首页推荐和启动弹窗。
- 管理公告、系统参数、功能开关、协议文档和 APP 版本策略。
- 上传运营图片素材。

### 4.6 反馈与风控

- 处理反馈工单和用户举报。
- 查看、确认或忽略风险事件。
- 设置用户观察、奖励限制、提现限制、冻结或封禁状态。
- 查看用户登录、设备和风险记录。

### 4.7 系统管理

- RBAC 角色与细粒度权限管理。
- 管理员账号及状态管理。
- 高风险操作要求当前管理员密码二次验证。
- 查看敏感操作审计日志。
- 后台内嵌 Swagger 接口文档入口。

## 5. 后端基础能力

- Express 模块化路由与 Prisma 数据访问层。
- Zod 请求参数校验和统一 JSON 响应结构。
- JWT 访问令牌、刷新令牌轮换和主动撤销。
- 登录失败锁定、接口限流、上传限流和财务接口限流。
- CORS、Helmet、响应压缩、请求 ID 和结构化访问日志。
- MySQL 事务、唯一索引和幂等请求号保护关键资金操作。
- BigInt 统一序列化，避免金币及金额精度丢失。
- 所有关键接口在路由源代码及 Swagger 中提供明确说明。

## 6. 默认本地开发配置

```text
MySQL 地址：127.0.0.1
数据库：huanyueapp
数据库账号：root
数据库密码：123456
后台账号：admin
后台密码：123456
```

以上仅用于本地开发。生产环境必须通过环境变量替换数据库密码、JWT 密钥、敏感数据加密密钥和管理员初始密码，并启用 HTTPS、对象存储及正式域名。

## 7. 当前待完成或待第三方确认事项

- 穿山甲内容输出正式接口、鉴权参数、分页映射和播放授权。
- 穿山甲广告收入回调字段、签名验证、回调 IP/证书策略和收入单位最终映射。
- 生产环境对象存储/CDN，当前头像和运营素材使用本地 `/uploads`。
- 短信验证码服务；当前主要使用手机号和密码登录。
- 支付宝、微信或银行真实打款通道；当前后台支持批次文件闭环。
- APP 正式下载域名、隐私合规文本、应用商店版本发布及灰度升级参数。
- 原需求中的 VIP 会员、真实支付订单和自动续费尚未实现。

## 8. 验收重点

1. 未登录用户无法进入任意业务页面。
2. 无邀请码不能注册，有效邀请码注册后只建立一次上级关系。
3. 用户独立广告比例为空时继承全局配置。
4. 每笔广告的观看者实际到账必须等于基础应分金币，两级返佣由平台在基础收益之外额外支付。
5. 缺少某一级上级时，平台不产生该级返佣，但观看者收益保持不变。
6. 同一个广告 `requestId` 重复提交不重复增加任何人的余额。
7. 提现冻结、拒绝退回、成功扣除及批次结果与金币流水保持一致。
8. 高风险后台操作必须通过权限和二次密码验证，并生成审计记录。

## 9. 广告收益运营与安全能力

- 管理后台“广告收益”页面集中展示广告收入、观看用户完整收益、平台支付的直推/间推返佣、平台结余金币等值及逐笔结算记录。
- 用户详情展示以当前用户为根节点的直推、间推团队，并汇总两级团队带来的返佣贡献。
- 穿山甲回调适配入口为 `POST /api/v1/webhooks/pangle/ad-revenue`；当前通用安全框架包含 HMAC-SHA256 签名、五分钟时效、随机串、可选 IP 白名单、事件幂等及成功/失败日志。
- 回调密钥使用 `PANGLE_CALLBACK_SECRET`，生产环境必须替换默认值；IP 白名单使用逗号分隔的 `PANGLE_CALLBACK_IPS`。
- 自动资金对账除余额与提现冻结额外，还检查观看者是否完整取得基础收益，以及观看者、直推和间推的结算金额是否存在匹配金币流水；历史记录继续按原拆分口径核对。
- 当前回调字段和签名串是内部适配协议；取得穿山甲正式文档后，只需调整 webhook 适配层，不改变内部结算公式和账务事务。

## 10. 设备环境风控评分

- 风控包含十项：SIM 卡、微信安装、抖音安装、支付宝安装、模拟器、云手机/云机、脚本环境、网络环境、IP 风险和位置距离。
- 每项结果分为 `PASS`、`RISK`、`UNKNOWN`：风险项扣 10 分，未知项不扣分；默认至少 6 项结果明确才允许自动判定，避免权限拒绝或 ROM 不兼容造成误封。低于 60 分自动封号，60 分不会自动封号。
- 自动封号会同时禁用账号、设置 `BANNED` 风险状态、撤销全部刷新令牌并产生严重风险事件；后续请求会被鉴权中间件拒绝。
- 后台“反薅与风控—设备环境评分”展示评分历史、设备 ID、IP、定位距离与自动封号结果，可由具备 `risk:update` 权限的管理员主动封号或解除封号。
- 坐标距离使用服务端 Haversine 公式计算；客户端不能直接指定最终分数或距离。
- Android APP 已接入 `hly-risk-collector` UTS 原生采集器，检测 SIM 可用性、三款应用安装状态、模拟器、云手机、调试/Root/Hook、VPN 和系统代理。IP 信誉与定位距离仍须接入服务端可信数据源，客户端不能自行判为安全。
- 每次上报前必须先申请两分钟有效的一次性随机挑战，挑战使用后立即失效，旧请求不能直接重放。

### 10.1 风控策略与关联账号

- 后台支持配置自动风控开关、自动判定最少有效项、自动封号阈值、预警阈值、最大地址距离、同设备账号阈值和同 IP 账号阈值，配置更新后立即生效并写入审计日志。
- 默认保持低于 60 分自动封号、低于 80 分预警、最大距离 50 公里、同设备 3 个账号预警、同 IP 10 个账号预警。
- 同设备或同 IP 达到阈值时生成独立高风险事件，供管理员结合邀请关系、收款账户和广告行为进一步处理。
- 风控中心展示检测总数、平均分、预警检测、自动封号和待处理事件统计。
- 封禁、冻结或奖励受限账号不能通过穿山甲回调或后台手工入口继续结算广告奖励。

### 10.2 检测时机与有效期

- 登录成功和 APP 冷启动后检查登录场景检测状态，默认有效期 24 小时。
- 用户进入广告任务前检查奖励场景检测状态，默认有效期 6 小时；过期时由已注册的原生安全采集器重新检测。
- 提交提现前检查提现场景检测状态，默认有效期 10 分钟。
- 后台“强制近期检测”开启后，广告收益结算和提现接口会在服务端再次校验有效期，不能通过绕过 APP 页面直接调用接口规避检测。
- “强制近期检测”默认关闭；H5 不会伪造设备检测数据。使用 HBuilderX 制作 Android 自定义基座并完成覆盖多品牌 ROM 的真机验证后，再从后台开启。

### 10.3 Android APK 发布要求

- 只支持 Android APK 侧载发布；UTS 插件需要通过 HBuilderX 自定义基座或云打包编译，H5 开发服务器只能验证业务流程，不能执行 Android 原生 API。
- 所有正式 APK 必须使用同一份受控发布证书签名，发布前执行 `apksigner verify --print-certs app-release.apk` 并核对证书 SHA-256。
- 当前一次性挑战可阻止普通重放，但客户端环境仍可能被 Hook。正式大规模放量前建议继续接入 Android Key Attestation 或商业设备指纹服务；证明无效或不支持时进入人工复核，不直接自动封号。
